import pillUtils from 'frontend-validation-tool/utils/pill-utils';
import { module, test } from 'qunit';

module('Unit | Utility | pillUtils', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = pillUtils();
    assert.ok(result);
  });
});
